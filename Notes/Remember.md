### AI
**Remember**
* To get feature-wise stats (mean, non‑zeros) across all samples → use axis=0.
* To get sample-wise stats (e.g. total activations per data point) → use axis=1.
### Numpy

**Remember**
* axis=0 means column-wise counting and axis=1 means row-wise counting
* 
**Problems**
